const frutas = ['Banana', 'Maçã', 'Abacaxi', 'Pêra', 'Uva'];

frutas.reverse();

const newString = frutas.join(", ");

console.log(newString);
//_____________________________________________________________________________

frutas.shift();
frutas.pop()
frutas.reverse();
frutas.push("Melão");

console.log(frutas);