const user = {
    givenName: "Jhon",
    age: 25,
    occupation: "Developer",
    favouriteColour: "green"
};

const {givenName, age, occupation, favouriteColour} = user;

console.log(`Hi, my name is ${givenName}, I'm ${age} years old. I'm ${occupation} and my favourite colour is ${favouriteColour}!`)
